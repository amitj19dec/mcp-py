# Enhanced MCP Client Backend Configuration with Azure AD Authentication

# Azure OpenAI Configuration (for chat functionality)
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# Azure AD Authentication for MCP Servers (Global defaults)
# These can be overridden per server in mcp_config.json
AZURE_TENANT_ID=your-tenant-id-here
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here

# Optional: Fallback to OpenAI if needed
# OPENAI_API_KEY=your-openai-key-here

# Optional: Logging Configuration
LOG_LEVEL=INFO

# Notes:
# - The client will authenticate to MCP servers using Azure AD client credentials flow
# - Each server in mcp_config.json can have its own auth configuration
# - Environment variables can be referenced in config using ${VAR_NAME} syntax
# - Servers without auth configuration will be accessed without authentication
