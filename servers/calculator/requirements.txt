# Official MCP Python SDK with CLI tools
mcp[cli]>=1.10.0

# Web server dependencies for streamable HTTP transport
uvicorn>=0.24.0
fastapi>=0.104.0

# Additional utilities
pydantic>=2.0.0
anyio>=4.0.0

# Azure AD and JWT dependencies for authentication
PyJWT[crypto]>=2.8.0
cryptography>=41.0.0
httpx>=0.25.0

# JWT Key Set handling for Azure AD JWKS (note: correct package name)
PyJWKS>=1.0.0
